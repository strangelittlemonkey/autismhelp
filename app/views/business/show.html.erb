<script>
    $.get('<%= @revtilt_url %>', function(data) {alert(data)})
</script>
<%= render 'business/navbar' %>
   <div class="row-fluid">
     <div class="span5">
<div class="all-info">
<div class="main-image">
<%= image_tag('placeholder.jpg') %>
</div>
<div class="business-info">
<h2>Gary Danko</h2>
  <% 4.times do |star| %>
  <%= image_tag('redstar.jpeg', :size => "20x20" ) %>
  <% end %>
<p class="attributes">Autism friendly | Category: Restaurants (American)</p>
</div>
</div>
<br>
 <h2 class="photos">Photos</h2>
<div class="photos">
    <% (JSON.parse(@photo_query.response.body)["response"]["photos"]["items"]).each do |photo| %>
      <div class="crop">
        <%= image_tag(@photo_url = photo["prefix"] + "cap250" + photo["suffix"]) %>
      </div>
    <% end %>
</div>
<div class="reviews">
  <h2>Reviews</h2>
</div>
</div>
</div>
